#Map: ! - impotant,
      ? - not impotant,
      ~ - need test,
      - - normal

"myinc:///SQL/SELECT/name#arg1&{myinc:///SQL/SELECT/agr2#arg3}&{myinc:///Window/name/Element/Text}&arg4" to
protocol:       myinc
host:           native
type:           SQL
SQL type:       SELECT
SQL Query name: name
Arguments:
                1) "arg1"
                2) result of myinc:///SQL/SELECT/agr2#arg3
                3) result of myinc:///Window/name/Element/Text
                4) "arg4"


1) load UI files from xml project (feature)
2) create Wizard "Create Project" or tool kit (feature)
3) add icons (feature)
4) add tool bar in MainWindow (feature)
5) add preview runed windows on Home Screen (feature)
6) finish debug menu (feature)
7) add math elements in URI
